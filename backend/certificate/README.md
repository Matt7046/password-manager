# Metti qui i certificati Mongo X509 (stessi di Activity Manager).
# Sul server: /root/password-manager/backend/certificate/
#
# File richiesti (nomi esatti):
#   client.pem
#   client-key.pem
#
# Copia da AM, ad esempio:
#   cp /root/app/backend/service/auth/src/main/resources/certificate/client.pem .
#   cp /root/app/backend/service/auth/src/main/resources/certificate/client-key.pem .
#
# NON commitare questi file su GitHub (sono già in .gitignore via *.pem).
