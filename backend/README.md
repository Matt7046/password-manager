# Backend Password Manager

API FastAPI pensata per girare sul server Docker di Activity Manager.

Vedi `../README.md` e `.env.EMPTY` per il deploy.

Health check: `GET /api/health` oppure `GET /api/auth/check`
